/**
 * Spotlight — DESIGN.md §8.2
 * Warm gold beam sweeping across a dark cocoa section. Transform + opacity only.
 * Pairs with the Signature Collection chapter over --bg-inverse (cocoa).
 */
export function Spotlight() {
  return <div className="spotlight" aria-hidden="true" />;
}
